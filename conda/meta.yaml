{% set name = "disCoverage" %}
{% set version = "0.1" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  path: ..

requirements:
  host:
    - python
  run:
    - python
    - python >=3.7
    - r-base
    - r-data.table
    - r-dplyr
    - r-readr
    - r-rmpfr
    - r-stringi
    - r-tibble
  
